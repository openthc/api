#
# Company Object
#

description: "A Company is a container for License and Contact models"
type: object
required: [ "id", "name" ]
properties:
  id:
    type: string
    description: Global/Universal Unique Identifier
  name:
    type: string
    description: Name of Company
  type:
    $ref: '#/components/schemas/Company_Type'
  flag:
    description: Bitflags of Company
    type: integer
