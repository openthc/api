#
# The Transfers
#

get:
  summary: Get Export Transfer List
  tags:
    - B2B Sales
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/Transfer'
