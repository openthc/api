#
# The Transfers
#

get:
  summary: Get Export Transfer List
  tags:
    - Transfer
  responses:
    '200':
      description: Success
      schema:
        type: array
        items:
          $ref: '#/components/schema/Transfer'
